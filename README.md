# demo-nextjs
A demo Next.js app with Google OAuth, PostgreSQL, and infinite scroll — fully tested with Dokkimi integration tests and a GitHub Actions CI pipeline. Shows how to mock external APIs, assert HTTP traffic, query databases, and run visual regression tests against a real Kubernetes environment.
