name: Dokkimi Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - name: Build Docker image
        run: docker build -t nextjs-demo:latest .

      - name: Run Dokkimi tests
        uses: dokkimi/github-action@v1.0.1
        with:
          tests: .dokkimi
          max-parallel: 3
          max-booting: 1

      - name: Extract version from package.json
        if: success()
        id: version
        run: echo "VERSION=$(jq -r '.version' package.json)" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        if: success()
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        if: success()
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        if: success()
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/dokkimi/nextjs-demo:latest
            ghcr.io/dokkimi/nextjs-demo:${{ steps.version.outputs.VERSION }}
          cache-from: type=registry,ref=ghcr.io/dokkimi/nextjs-demo:buildcache
          cache-to: type=registry,ref=ghcr.io/dokkimi/nextjs-demo:buildcache,mode=max

      - name: Make image public
        if: success()
        run: |
          gh api user/packages/container/nextjs-demo/access_levels -X PATCH -f visibility=public
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
